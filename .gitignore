# Dependencies
node_modules/
.pnpm-store

# Production build output
dist/
build/
build_win/
release/
MC-Music-Setup-*.exe

# Downloaded Binaries & Temporary Files
bin/ffmpeg*
bin/yt-dlp*
scratch/
tmp/
*.log

# Environment variables & Local config
.env
.env.local
.env.development.local
.env.production.local

# OS generated files
.DS_Store
Thumbs.db

# Android / Capacitor generated files
android/.gradle/
android/.idea/
android/build/
android/app/build/
android/capacitor-cordova-android-plugins/build/
android/local.properties
android/keystore.properties
android/*.keystore
android/*.jks

# Installers
# Android APK is ~10MB (tracked for direct GitHub download)
!Instaladores/Android_Movil/*.apk

# Windows EXE is ~129MB (exceeds GitHub 100MB limit, published via GitHub Releases)
Instaladores/Windows_PC/*.exe

